import React from 'react';
import LoginAdmin from './loginAdmin';

const page = () => {
    return (
        <section className=' h-screen'>
            <LoginAdmin />
        </section>
    );
};

export default page;